{
  "insert_sample": {
    "postgres": "INSERT INTO synapse_fluid_dynamics (collected_at, scenario_name, intersection_id, edge_id, density, mean_speed, min_speed, queue_length, max_queue, occupancy, edge_length, num_lanes, speed_limit, maturity_stage) VALUES (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s);",
    "sqlite": "INSERT INTO synapse_fluid_dynamics (collected_at, scenario_name, intersection_id, edge_id, density, mean_speed, min_speed, queue_length, max_queue, occupancy, edge_length, num_lanes, speed_limit, maturity_stage) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?);"
  },
  "query_history": {
    "postgres": "SELECT edge_id, density, mean_speed, queue_length, occupancy, edge_length, num_lanes, speed_limit, collected_at, maturity_stage, scenario_name, intersection_id, min_speed, max_queue FROM synapse_fluid_dynamics WHERE collected_at >= %s;",
    "sqlite": "SELECT edge_id, density, mean_speed, queue_length, occupancy, edge_length, num_lanes, speed_limit, collected_at, maturity_stage, scenario_name, intersection_id, min_speed, max_queue FROM synapse_fluid_dynamics WHERE collected_at >= ?;",
    "all": "SELECT edge_id, density, mean_speed, queue_length, occupancy, edge_length, num_lanes, speed_limit, collected_at, maturity_stage, scenario_name, intersection_id, min_speed, max_queue FROM synapse_fluid_dynamics;"
  },
  "query_aggregated": {
    "postgres": "SELECT edge_id, SUM(density * (mean_speed * 3.6)) AS volume_sum, COUNT(*) AS volume_cnt, SUM(CASE WHEN mean_speed > 0.1 THEN GREATEST(0.0, (edge_length / mean_speed) - (edge_length / COALESCE(NULLIF(speed_limit, 0), 13.89))) ELSE 0.0 END) AS delay_sum, COUNT(CASE WHEN mean_speed > 0.1 THEN 1 END) AS delay_cnt, AVG(queue_length) AS avg_queue, MAX(queue_length) AS max_queue, MAX(edge_length) AS edge_length, MAX(num_lanes) AS num_lanes, MAX(speed_limit) AS speed_limit, COUNT(*) AS total_samples FROM synapse_fluid_dynamics WHERE collected_at >= %s GROUP BY edge_id;",
    "sqlite": "SELECT edge_id, SUM(density * (mean_speed * 3.6)) AS volume_sum, COUNT(*) AS volume_cnt, SUM(CASE WHEN mean_speed > 0.1 THEN MAX(0.0, (edge_length / mean_speed) - (edge_length / COALESCE(NULLIF(speed_limit, 0), 13.89))) ELSE 0.0 END) AS delay_sum, COUNT(CASE WHEN mean_speed > 0.1 THEN MAX(0.0, (edge_length / mean_speed) - (edge_length / COALESCE(NULLIF(speed_limit, 0), 13.89))) ELSE 0.0 END) AS delay_cnt, AVG(queue_length) AS avg_queue, MAX(queue_length) AS max_queue, MAX(edge_length) AS edge_length, MAX(num_lanes) AS num_lanes, MAX(speed_limit) AS speed_limit, COUNT(*) AS total_samples FROM synapse_fluid_dynamics WHERE collected_at >= ? GROUP BY edge_id;",
    "postgres_all": "SELECT edge_id, SUM(density * (mean_speed * 3.6)) AS volume_sum, COUNT(*) AS volume_cnt, SUM(CASE WHEN mean_speed > 0.1 THEN GREATEST(0.0, (edge_length / mean_speed) - (edge_length / COALESCE(NULLIF(speed_limit, 0), 13.89))) ELSE 0.0 END) AS delay_sum, COUNT(CASE WHEN mean_speed > 0.1 THEN 1 END) AS delay_cnt, AVG(queue_length) AS avg_queue, MAX(queue_length) AS max_queue, MAX(edge_length) AS edge_length, MAX(num_lanes) AS num_lanes, MAX(speed_limit) AS speed_limit, COUNT(*) AS total_samples FROM synapse_fluid_dynamics GROUP BY edge_id;",
    "sqlite_all": "SELECT edge_id, SUM(density * (mean_speed * 3.6)) AS volume_sum, COUNT(*) AS volume_cnt, SUM(CASE WHEN mean_speed > 0.1 THEN MAX(0.0, (edge_length / mean_speed) - (edge_length / COALESCE(NULLIF(speed_limit, 0), 13.89))) ELSE 0.0 END) AS delay_sum, COUNT(CASE WHEN mean_speed > 0.1 THEN MAX(0.0, (edge_length / mean_speed) - (edge_length / COALESCE(NULLIF(speed_limit, 0), 13.89))) ELSE 0.0 END) AS delay_cnt, AVG(queue_length) AS avg_queue, MAX(queue_length) AS max_queue, MAX(edge_length) AS edge_length, MAX(num_lanes) AS num_lanes, MAX(speed_limit) AS speed_limit, COUNT(*) AS total_samples FROM synapse_fluid_dynamics GROUP BY edge_id;"
  },
  "fallback_history_batches": {
    "postgres": "SELECT edge_id, avg_density AS density, avg_speed AS mean_speed, avg_queue AS queue_length, avg_occupancy AS occupancy, 100.0 AS edge_length, 1 AS num_lanes, 50.0 AS speed_limit, summary_hour AS collected_at FROM synapse_edge_phase_hourly_summary WHERE summary_hour >= %s ORDER BY summary_hour ASC;",
    "sqlite": "SELECT edge_id, avg_density AS density, avg_speed AS mean_speed, avg_queue AS queue_length, avg_occupancy AS occupancy, 100.0 AS edge_length, 1 AS num_lanes, 50.0 AS speed_limit, summary_hour AS collected_at FROM synapse_edge_phase_hourly_summary WHERE summary_hour >= ? ORDER BY summary_hour ASC;",
    "all": "SELECT edge_id, avg_density AS density, avg_speed AS mean_speed, avg_queue AS queue_length, avg_occupancy AS occupancy, 100.0 AS edge_length, 1 AS num_lanes, 50.0 AS speed_limit, summary_hour AS collected_at FROM synapse_edge_phase_hourly_summary ORDER BY summary_hour ASC;"
  },
  "fallback_aggregated": {
    "postgres": "SELECT edge_id, SUM(avg_density * avg_speed * sample_count) AS volume_sum, SUM(sample_count) AS volume_cnt, 0.0 AS delay_sum, SUM(sample_count) AS delay_cnt, AVG(avg_queue) AS avg_queue, MAX(max_queue) AS max_queue, 100.0 AS edge_length, 1 AS num_lanes, 50.0 AS speed_limit, SUM(sample_count) AS total_samples FROM synapse_edge_phase_hourly_summary WHERE summary_hour >= %s GROUP BY edge_id;",
    "sqlite": "SELECT edge_id, SUM(avg_density * avg_speed * sample_count) AS volume_sum, SUM(sample_count) AS volume_cnt, 0.0 AS delay_sum, SUM(sample_count) AS delay_cnt, AVG(avg_queue) AS avg_queue, MAX(max_queue) AS max_queue, 100.0 AS edge_length, 1 AS num_lanes, 50.0 AS speed_limit, SUM(sample_count) AS total_samples FROM synapse_edge_phase_hourly_summary WHERE summary_hour >= ? GROUP BY edge_id;",
    "all": "SELECT edge_id, SUM(avg_density * avg_speed * sample_count) AS volume_sum, SUM(sample_count) AS volume_cnt, 0.0 AS delay_sum, SUM(sample_count) AS delay_cnt, AVG(avg_queue) AS avg_queue, MAX(max_queue) AS max_queue, 100.0 AS edge_length, 1 AS num_lanes, 50.0 AS speed_limit, SUM(sample_count) AS total_samples FROM synapse_edge_phase_hourly_summary GROUP BY edge_id;"
  },
  "purge_old": {
    "postgres": "DELETE FROM synapse_fluid_dynamics WHERE collected_at < NOW() - INTERVAL '%s minutes';",
    "sqlite": "DELETE FROM synapse_fluid_dynamics WHERE collected_at < datetime('now', ? || ' minutes');"
  },
  "get_min_collected_before": {
    "postgres": "SELECT MIN(collected_at) FROM synapse_fluid_dynamics WHERE collected_at < NOW() - INTERVAL '%s hours';"
  },
  "get_cutoff_timestamp_interval": {
    "postgres": "SELECT (NOW() - INTERVAL '%s hours')::timestamp;"
  },
  "get_next_batch_timestamp": {
    "postgres": "SELECT (%s::timestamp + INTERVAL '%s days')::timestamp;"
  },
  "consolidate_edge_summary": {
    "postgres": "INSERT INTO synapse_edge_phase_hourly_summary (edge_id, maturity_stage, summary_hour, scenario_name, sample_count, avg_speed, min_speed, avg_density, avg_queue, max_queue, total_production, avg_occupancy) SELECT edge_id, maturity_stage, date_trunc('hour', collected_at) AS summary_hour, scenario_name, SUM(COALESCE(sample_count, 1)) AS sample_count, SUM(mean_speed * COALESCE(sample_count, 1)) / SUM(COALESCE(sample_count, 1)) AS avg_speed, MIN(COALESCE(min_speed, mean_speed)) AS min_speed, SUM(density * COALESCE(sample_count, 1)) / SUM(COALESCE(sample_count, 1)) AS avg_density, SUM(queue_length * COALESCE(sample_count, 1)) / SUM(COALESCE(sample_count, 1)) AS avg_queue, MAX(COALESCE(max_queue, queue_length)) AS max_queue, SUM(density * mean_speed * COALESCE(edge_length, 100.0) * COALESCE(sample_count, 1)) AS total_production, SUM(occupancy * COALESCE(sample_count, 1)) / SUM(COALESCE(sample_count, 1)) AS avg_occupancy FROM synapse_fluid_dynamics WHERE collected_at >= %s AND collected_at < %s GROUP BY edge_id, maturity_stage, date_trunc('hour', collected_at), scenario_name ON CONFLICT (edge_id, maturity_stage, summary_hour) DO UPDATE SET sample_count = EXCLUDED.sample_count, avg_speed = EXCLUDED.avg_speed, min_speed = EXCLUDED.min_speed, avg_density = EXCLUDED.avg_density, avg_queue = EXCLUDED.avg_queue, max_queue = EXCLUDED.max_queue, total_production = EXCLUDED.total_production, avg_occupancy = EXCLUDED.avg_occupancy;",
    "sqlite": "INSERT OR REPLACE INTO synapse_edge_phase_hourly_summary (edge_id, maturity_stage, summary_hour, scenario_name, sample_count, avg_speed, min_speed, avg_density, avg_queue, max_queue, total_production, avg_occupancy) SELECT edge_id, maturity_stage, strftime('%Y-%m-%d %H:00:00', collected_at) AS summary_hour, scenario_name, SUM(COALESCE(sample_count, 1)) AS sample_count, AVG(mean_speed) AS avg_speed, MIN(COALESCE(min_speed, mean_speed)) AS min_speed, AVG(density) AS avg_density, AVG(queue_length) AS avg_queue, MAX(COALESCE(max_queue, queue_length)) AS max_queue, SUM(density * mean_speed * COALESCE(edge_length, 100.0)) AS total_production, AVG(occupancy) AS avg_occupancy FROM synapse_fluid_dynamics WHERE collected_at < datetime('now', ? || ' hours') GROUP BY edge_id, maturity_stage, strftime('%Y-%m-%d %H:00:00', collected_at), scenario_name;"
  },
  "consolidate_intersection_summary": {
    "postgres": "INSERT INTO synapse_intersection_phase_hourly_summary (intersection_id, maturity_stage, summary_hour, scenario_name, sample_count, avg_speed, min_speed, avg_queue, max_queue, total_production, total_delay) SELECT COALESCE(intersection_id, 'unassigned') AS intersection_id, maturity_stage, date_trunc('hour', collected_at) AS summary_hour, scenario_name, SUM(COALESCE(sample_count, 1)) AS sample_count, SUM(mean_speed * COALESCE(sample_count, 1)) / SUM(COALESCE(sample_count, 1)) AS avg_speed, MIN(COALESCE(min_speed, mean_speed)) AS min_speed, SUM(queue_length * COALESCE(sample_count, 1)) / SUM(COALESCE(sample_count, 1)) AS avg_queue, MAX(COALESCE(max_queue, queue_length)) AS max_queue, SUM(density * mean_speed * COALESCE(edge_length, 100.0) * COALESCE(sample_count, 1)) AS total_production, SUM(CASE WHEN mean_speed > 0.1 THEN GREATEST(0.0, (COALESCE(edge_length, 100.0) / mean_speed) - (COALESCE(edge_length, 100.0) / COALESCE(NULLIF(speed_limit, 0), 13.89))) * COALESCE(sample_count, 1) ELSE 0.0 END) AS total_delay FROM synapse_fluid_dynamics WHERE collected_at >= %s AND collected_at < %s GROUP BY COALESCE(intersection_id, 'unassigned'), maturity_stage, date_trunc('hour', collected_at), scenario_name ON CONFLICT (intersection_id, maturity_stage, summary_hour) DO UPDATE SET sample_count = EXCLUDED.sample_count, avg_speed = EXCLUDED.avg_speed, min_speed = EXCLUDED.min_speed, avg_queue = EXCLUDED.avg_queue, max_queue = EXCLUDED.max_queue, total_production = EXCLUDED.total_production, total_delay = EXCLUDED.total_delay;",
    "sqlite": "INSERT OR REPLACE INTO synapse_intersection_phase_hourly_summary (intersection_id, maturity_stage, summary_hour, scenario_name, sample_count, avg_speed, min_speed, avg_queue, max_queue, total_production, total_delay) SELECT COALESCE(intersection_id, 'unassigned') AS intersection_id, maturity_stage, strftime('%Y-%m-%d %H:00:00', collected_at) AS summary_hour, scenario_name, SUM(COALESCE(sample_count, 1)) AS sample_count, AVG(mean_speed) AS avg_speed, MIN(COALESCE(min_speed, mean_speed)) AS min_speed, AVG(queue_length) AS avg_queue, MAX(COALESCE(max_queue, queue_length)) AS max_queue, SUM(density * mean_speed * COALESCE(edge_length, 100.0)) AS total_production, SUM(0.0) AS total_delay FROM synapse_fluid_dynamics WHERE collected_at < datetime('now', ? || ' hours') GROUP BY COALESCE(intersection_id, 'unassigned'), maturity_stage, strftime('%Y-%m-%d %H:00:00', collected_at), scenario_name;"
  },
  "purge_consolidated_window": {
    "postgres": "DELETE FROM synapse_fluid_dynamics WHERE collected_at >= %s AND collected_at < %s;",
    "sqlite": "DELETE FROM synapse_fluid_dynamics WHERE collected_at < datetime('now', ? || ' hours');"
  },
  "get_count": "SELECT COUNT(*) FROM synapse_fluid_dynamics;",
  "get_count_summary": "SELECT COUNT(*) FROM synapse_edge_phase_hourly_summary;",
  "get_max_collected_at": "SELECT MAX(collected_at) FROM synapse_fluid_dynamics;",
  "get_max_summary_hour": "SELECT MAX(summary_hour) FROM synapse_edge_phase_hourly_summary;",
  "get_min_max_summary_hour": "SELECT MIN(summary_hour), MAX(summary_hour) FROM synapse_edge_phase_hourly_summary;",
  "get_min_max_time": {
    "postgres": "SELECT MIN(collected_at), MAX(collected_at) FROM synapse_fluid_dynamics WHERE collected_at >= %s;",
    "sqlite": "SELECT MIN(collected_at), MAX(collected_at) FROM synapse_fluid_dynamics WHERE collected_at >= ?;",
    "all": "SELECT MIN(collected_at), MAX(collected_at) FROM synapse_fluid_dynamics;"
  },
  "get_earliest_child_sfd": "SELECT MIN(collected_at) FROM synapse_fluid_dynamics WHERE maturity_stage = 'CHILD';",
  "get_earliest_sfd": "SELECT MIN(collected_at) FROM synapse_fluid_dynamics;",
  "get_earliest_child_summary": "SELECT MIN(summary_hour) FROM synapse_edge_phase_hourly_summary WHERE maturity_stage = 'CHILD';",
  "get_earliest_summary": "SELECT MIN(summary_hour) FROM synapse_edge_phase_hourly_summary;",
  "get_earliest_checkpoint": "SELECT MIN(last_updated) FROM cloud_file_vault WHERE relative_path LIKE '%checkpoints/%.pth' OR relative_path LIKE '%.pth';",
  "mfd_reconstruct_sfd": {
    "postgres": "SELECT collected_at, edge_id, density, mean_speed, queue_length, occupancy, edge_length, maturity_stage FROM (SELECT collected_at, edge_id, density, mean_speed, queue_length, occupancy, edge_length, maturity_stage FROM synapse_fluid_dynamics WHERE collected_at >= %s ORDER BY collected_at DESC LIMIT 50000) SUB ORDER BY collected_at ASC;",
    "sqlite": "SELECT collected_at, edge_id, density, mean_speed, queue_length, occupancy, edge_length, maturity_stage FROM (SELECT collected_at, edge_id, density, mean_speed, queue_length, occupancy, edge_length, maturity_stage FROM synapse_fluid_dynamics WHERE collected_at >= ? ORDER BY collected_at DESC LIMIT 50000) SUB ORDER BY collected_at ASC;",
    "all": "SELECT collected_at, edge_id, density, mean_speed, queue_length, occupancy, edge_length, maturity_stage FROM (SELECT collected_at, edge_id, density, mean_speed, queue_length, occupancy, edge_length, maturity_stage FROM synapse_fluid_dynamics ORDER BY collected_at DESC LIMIT 50000) SUB ORDER BY collected_at ASC;"
  },
  "mfd_reconstruct_summary": {
    "postgres": "SELECT summary_hour AS collected_at, edge_id, avg_density AS density, avg_speed AS mean_speed, avg_queue AS queue_length, avg_occupancy AS occupancy, 100.0 AS edge_length, maturity_stage FROM (SELECT summary_hour, edge_id, avg_density, avg_speed, avg_queue, avg_occupancy, maturity_stage FROM synapse_edge_phase_hourly_summary WHERE summary_hour >= %s ORDER BY summary_hour DESC LIMIT 50000) SUB ORDER BY summary_hour ASC;",
    "sqlite": "SELECT summary_hour AS collected_at, edge_id, avg_density AS density, avg_speed AS mean_speed, avg_queue AS queue_length, avg_occupancy AS occupancy, 100.0 AS edge_length, maturity_stage FROM (SELECT summary_hour, edge_id, avg_density, avg_speed, avg_queue, avg_occupancy, maturity_stage FROM synapse_edge_phase_hourly_summary WHERE summary_hour >= ? ORDER BY summary_hour DESC LIMIT 50000) SUB ORDER BY summary_hour ASC;",
    "all": "SELECT summary_hour AS collected_at, edge_id, avg_density AS density, avg_speed AS mean_speed, avg_queue AS queue_length, avg_occupancy AS occupancy, 100.0 AS edge_length, maturity_stage FROM (SELECT summary_hour, edge_id, avg_density, avg_speed, avg_queue, avg_occupancy, maturity_stage FROM synapse_edge_phase_hourly_summary ORDER BY summary_hour DESC LIMIT 50000) SUB ORDER BY summary_hour ASC;"
  }
}
