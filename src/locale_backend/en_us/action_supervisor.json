{
  "action_supervisor": {
    "init": {
      "actuator_created": "ActionSupervisor initialized with ConnectionManager."
    },
    "veto_blocked": "[{tl_id}] Action blocked by Guardian veto.",
    "override_active": "[{tl_id}] Skipping send_stage_hold because of active override: {state}",
    "shutdown_global": "[ActionSupervisor] Global SHUTDOWN: Stopping heartbeat for traffic light {ip}",
    "flash_sent": "[{tl_id}] FLASH (Alert) command sent to hardware via ActionSupervisor.",
    "dark_sent": "[{tl_id}] DARK (Off) command sent to hardware via ActionSupervisor.",
    "normal_returned": "[{tl_id}] Traffic light returned to normal operation by operator."
  }
}